# Odin 3 Desktop

AYN Odin 3 的 Android 桌面应用，使用 Kotlin、Jetpack Compose 和 Gradle Wrapper。

## 项目约定

用户于 2026-09-04 授权“以后本项目全部自动”：在指定任务范围内自主完成实现、修复、必要验证、文档、Git 提交和推送，无需逐步重复确认；用户后续具体指令优先。

- 代码、测试和文档默认提交并正常推送到 [LeXwDeX/odin3-desktop](https://github.com/LeXwDeX/odin3-desktop) 的 `origin/main`，完成后核对远端提交与本地状态。保留已有修改；强制推送、覆盖他人历史和清空用户数据不在持续授权范围内。
- 用户于 2026-09-07 要求后续代码迭代默认完成 GitHub Release 和掌机更新：推送新版本后等待发布结果，校验正式 APK，并保留数据覆盖安装到已确认连接的目标掌机；设备未连接时明确记录待安装版本。发布、签名校验、设备验收和失败处理均按 [APK 发布](docs/releases.md) 执行，不以“已触发”代替“已安装”。
- 签名私钥、配对令牌、账户凭据、游戏与存档不进入 Git。
- 主代理为 GPT-6（Astra）时，具体编码任务的子代理模型可默认选择 GPT-5.6（Sol 或 Terra）。

## 操作前后校验

修改代码、文件、设备或外部服务状态时，按以下顺序校验；深度与影响范围、不可逆程度相匹配。身份、范围和证据齐备且已有授权时自主执行，无需逐步重复确认。

1. **界定范围**：把用户意图对应到具体对象、动作、预期结果和必须保留项。“全部”“永久删除”等表述仍受原始任务范围约束；用户认可数量或候选清单，不替代对象身份核实。
2. **核实身份**：包名、路径、进程名等标识只能定位对象，不能单凭名称推断用途。以当前环境元数据核对其真实身份；删除、卸载、覆盖或停止服务前，还须用独立来源交叉核对，如应用显示名与发布方、文件内容与归属、进程可执行文件与所属服务。文档、Skill、记忆中的映射仅作线索。
3. **固定执行目标**：执行前刷新设备、账户、目录、版本或提交等相关状态，保存必要的非敏感基线。批量操作逐项校验，并将命令限定到已核实目标；身份或范围有歧义时暂停该项，先补证，仍无法确定时才向用户澄清。
4. **核对附带影响**：检查参数是否连带清除应用数据、递归处理子目录、影响共享资源或覆盖已有修改。实际影响必须落在授权范围内；恢复方式遵循当前授权，永久删除须有明确指示。
5. **独立验收**：分别证明对象符合用户意图、预期变化实际发生、相关保留项未受影响。验收依据包含用户目标、执行前基线及执行后的独立读回，不能仅复用执行脚本的候选清单自证正确。返回 `Success`、退出码为零或目标消失只证明命令结果；按需验证实际运行行为，并保留可复核的非敏感差异。
6. **处理偏差**：发现身份错误、非预期变化或证据冲突，立即暂停依赖该判断的后续操作，报告实际影响和未确认项，在授权范围内补救。仅按已验证的层次报告完成；重新安装、配置恢复与用户数据恢复分别核验。

## 按需阅读

- 构建、验证或设备调试前，读 [开发与设备验证](docs/development.md)。
- 修改性能、风扇或 Home/返回行为前，读 [修复与交接记录](docs/performance-fan-home-fixes.md)，包括设备重连后的待验项。
- 修改、启动或排查硬件桥前，读 [桥接说明](tools/hardware-bridge/README.md)。
- 功能与交互说明见 [README](README.md)。

<!-- codebase-memory-mcp:start -->
## 代码发现

代码结构查询优先使用 `codebase-memory-mcp`。会话开始或压缩恢复后，先读 [图谱验证流程](docs/code-discovery.md)，确认最近项目与索引代次；默认采用 Tier 2。每个作为证据的文件都要检查索引覆盖，存在缺口时直接核验对应源码。
<!-- codebase-memory-mcp:end -->
