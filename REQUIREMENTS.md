# Odin 3 专属应用（odin3_desktop）需求梗概与开发规范

## 一、 项目背景与定位
- **定位**：完全取代 AYN Odin 3 官方桌面（Default Home Launcher），专为掌机手柄操作量身打造的掌机启动台与系统增强应用。
- **形态**：单应用集成 Launcher 桌面、桌面控制台与后台策略服务。滤镜和息屏挂机当前通过通知栏快捷磁贴测试，最终统一集成到桌面控制台。
- **能力边界**：桌面、截图滤镜预览和挂机黑色浮层采用 Android 标准能力。完整跨应用实时滤镜仍需原生渲染接入；迁移入口不代表完成 Hook 或取得硬件控制权限。
- **功能变更记录**：
  - 取消“后台一键强杀（kill/force-stop）”功能。
  - 取消内存清理与磁盘清理，不再提供相应按钮或确认弹窗。
  - 首页最左侧新增固定 Dashboard，集成只读状态、四项常用操作和独立截图滤镜调整。
  - **2026-09-04 用户确认**：先完善滤镜等功能，最后统一迁移到桌面控制台；现阶段保留下拉入口用于测试。

### 开发顺序与最终入口

1. 先完善截图导入与实时预览、滤镜组合、参数生效提示、保存恢复、手柄操作及息屏挂机。无游戏时也能独立调节截图。
2. 参数存储、渲染和挂机服务保持可由不同入口调用；功能是否可用取决于后端实际能力，界面如实显示。
3. 功能验证完成后，再集成到桌面控制台，复用已有截图和应用配置。当前先增加 Dashboard 滤镜入口；游戏内呼出和其余入口迁移另行完善。
4. 控制台入口验证完成后，移除正式使用的下拉磁贴入口。现阶段不扩展磁贴专属功能，也不提前撤掉测试入口。

---

## 二、 核心功能规格说明

### 1. 桌面启动台与 Tab 管理引擎（核心领域层）
- **默认桌面**：声明 `android.intent.category.HOME` 与 `android.intent.category.DEFAULT`，支持系统设置设为默认启动器。
- **Tab 分组持久化**：
  - 本地 SQLite (Room) 存储 Tab 配置与应用归类关系。
  - 支持 Tab 的完整 CRUD（新建、删除、重命名、排序、默认选中）。
  - 支持已安装应用的扫描（`QUERY_ALL_PACKAGES`）、归类入 Tab、自定排序、隐藏不需要的应用。
- **Dashboard**：固定在全部用户分类的左侧，独立于数据库 Tab，不能删除或排序；进程首次进入显示 Dashboard，已有分类、默认分类标记和应用映射保持原样。
- **异步监测**：磁盘分类独立后台加载；内存、CPU/GPU 温度、外部卷容量和 Wi-Fi 周期读取。离开 Dashboard 或桌面不可见时停止采样，不等待统计才显示首页。不采样 CPU/GPU 使用率。
- **展示内容**：内置磁盘系统/应用/其他/空闲分类；总内存和非系统应用进程 PSS；CPU、GPU 各自的真实温度；Wi-Fi 名称和上传/下载速率。温度不可读取时显示“— °C”，不能用电池温度或固定值代替；其他未知指标同样不能画成零占用。
- **温度条**：CPU/GPU 卡片保留温度数值，温度条统一 0–105°C 并标出两端 `0°`、`105°`；条长限制在范围内，数值仍显示实际读数。未知温度显示空轨道。青、暖、红色只作温度阅读提示，不代表 OEM 硬件保护阈值。
- **外部存储布局**：无外部卷时显示单个内置存储卡片；有外部卷时，内置存储与每个已挂载卷分别展示。外部卷显示名称、总量、已用、空闲及只读状态，不与内置容量合并，不套用系统/应用分类。多卷可增加行数，内容可滚动。
- **统计与操作分开**：统计卡片只读，不接收点击或手柄焦点，不显示“A 详情”或“点击授权”。宽屏中存储区域和 CPU/GPU 组合共用左右边界，RAM 与 Wi-Fi 共用另一列边界。
- **常用功能**：仅文件管理、系统设置、Odin 设置、滤镜调整四项，按此顺序排列；宽屏四个等宽按钮，窄屏可分两行。其下保留全局硬件 Dock，方向键下移可进入并按 A 操作。
- **滤镜入口**：Dashboard 打开独立截图配置，不要求前台游戏，可更换截图；下拉测试入口暂时保留。

### 2. 息屏挂机与 OLED 防烧屏（AFK 引擎）
- **入口**：
  - 当前用 `TileService` 在下拉控制中心提供“息屏挂机”测试开关；最终由桌面控制台调用同一挂机服务。
- **全屏纯黑覆盖**：
  - 申请 `SYSTEM_ALERT_WINDOW` 权限，以 `TYPE_APPLICATION_OVERLAY` 弹出全屏遮罩。
  - 背景采用纯黑 `#000000`，遮住游戏画面；Android 显示屏保持开启，不宣称物理熄屏或固定功耗。
  - 覆盖层拉低系统亮度（`screenBrightness = 0.01f`）。
- **防烧屏像素漂移（Anti-Burn-In Pixel Shift）**：
  - 挂机层展示暗灰色（如 `#333333`）极简状态（时间、电量、挂机时长、温度）。
  - 内置不规则漫游算法：周期性（如每 30 秒）在一定范围内随机平移渲染坐标，避免固定像素连续发光造成 OLED 残影或烧屏。
- **保持运行与防误触**：
  - 持有有时限的 `PARTIAL_WAKE_LOCK` 防止 CPU 休眠，并保持游戏窗口焦点；不保证游戏全速或固定帧率。
  - 拦截普通触屏手势以防误触游戏，双击屏幕退出；提示与实际退出方式一致。

### 3. 风扇充电模式（Fan Watchdog）
- **开关与手动优先**：仅在用户启用“充电模式”时执行策略。Dock 风扇聚焦时 X 切换，触摸副行也可操作；A 手动选档先关闭充电模式，避免后台策略覆盖用户选择。停用或服务结束时不写回过时的启动档位。
- **允许停转的条件**：正在充电、前台应用已识别且不是游戏、性能为默认档、CPU/GPU 温度读取有效且最高温不超过 60°C。温度或应用信息读取失败时禁止自动关风扇。
- **散热恢复**：进入游戏或温度超过 60°C 时使用智能档，已有高性能风扇档保持；拔电、前台不明或提高性能档时，只恢复由本策略主动关闭的风扇。读取失败后也尝试恢复本策略关闭的风扇。
- **硬件映射**：当前三档写入为 `fan_mode` 关闭 `0`、智能 `4`、高性能 `5`。由已获授权的后端执行并读回确认，不以界面动画代表写入成功。
- **性能联动**：手动最大跨默认、性能、高性能三档保持；其他手动状态切回默认用关闭，升到性能或高性能用智能。传感器开关跨性能切换保留：高档使用智能冷却，回默认由充电、前台及温度条件判断。联动通过同一个后端事务执行，处理 SystemUI 的异步风扇覆盖，并核对配置及 PWM。
- **并发与显示**：已开始的性能/风扇事务完成读回后再执行后续操作；同类待处理按键合并，按用户要求即时显示所选目标，不展示中间态；后台执行最后一次意图，失败时提示并校正。旧回包不得覆盖新选择。自动策略写入前重新验证控制版本、性能、风扇和前台请求。系统外部改档或策略停转需刷新 Dock；无法确认时显示“未读取”。
- **失败与生命周期**：策略串行执行，写入前复核开关与请求是否仍有效；异常不能导致服务崩溃。连续失败按 30 秒至 5 分钟退避，销毁时取消任务，不持续快速重试。该策略不替代 OEM thermal 服务。

### 4. 桌面 UI/UE 与手柄焦点引擎
- **顶部 Tab 栏 (`TopTabBar`)**：
  - 物理 `L1 / R1` 肩键循环切换 Dashboard 与应用分类；方向键在顶部可选择右侧 `[ ⚙️ CONFIG ]`。
  - 全局默认 `A` 确认，`B` 取消/返回；方向键移动焦点。`X/Y` 用于辅助操作。
  - 滤镜页仅主层（参数栏可见、高级参数未展开）允许 `L1/R1` 切预设，选择弹层和滑条调节中不切换。
  - 滤镜滑条聚焦后按 `A` 进入调节，左右修改；`A` 确认，`B` 取消并恢复本次进入前的数值。选择弹层方向键选项、`A` 确认、`B` 取消；一次按键只处理当前层。
  - 滤镜 `B` 按当前层依次关闭选择弹层、取消滑条调节、收起高级参数、隐藏参数栏，最后退出页面。
  - 按住 `X` 看原图，按住 `Y` 隐藏参数，松开各自恢复；弹层内同样支持。失焦或离开页面也恢复，不保存临时隐藏状态。
- **中部应用展示区 (`AppHorizontalRow` & `AppCard`)**：
  - 顶部大字独立展示 `HOVERED APP NAME`，消除长名称截断。
  - 单行大卡片水平滚动，焦点居中，卡片聚焦 1.10x 放大并带有青色流光边框。
  - 实体 `A` 键启动应用，长按 `X` 管理分类应用；短按 `Y` 排序，长按 `Y` 打开应用操作菜单。
- **底部硬件状态控制栏 (`BottomDockBar`)**：
  - 五个等宽卡片，手柄聚焦显示清晰青色边框；方向键进入后按 A 与触摸主卡片行为一致：
    1. 性能：A 循环“默认 → 性能 → 高性能”。
    2. 风扇：A 循环“关闭 → 智能 → 高性能”；副行显示“X 充电模式 开/关”。
    3. 摇杆灯：A 开关。
    4. 充电限制：A 同时切换 80% 上限和 5V 档，主副行分别显示两项真实读回状态；两项未同时开启时，下一次操作尝试同时开启。5V 档不表示实测电流为 3A，实际电流取决于电源与协商。
    5. 飞行模式：A 开关；权限不足或读回失败应提示，不能伪造成功状态。
- **系统设置弹窗 (`ConfigDialog`)**：
  - 1、摇杆灯颜色选择（色环调色预设，写回 `joystick_led_light_picker_color`）。
  - 2、屏幕方向规则（针对 Odin 3 底侧 USB 物理接口，仅支持 固定横屏 与 传感器横屏 两种模式）。
  - 3、风扇充电模式开关。
  - 4、Tab 页编辑（增删改查 Tab，上限 10 个）。
  - 5、关于（Odin 3 专属应用、开源许可说明）。
- **屏蔽原厂右侧侧滑菜单（GameAssistant）**：
  - 提供通过 ADB 命令禁用原厂侧滑悬浮窗的指引（`pm disable-user com.odin.gameassistant`），实现干净清爽的界面体验，且随时可逆。

### 5. 硬件后端与验收边界
- 普通应用通过当前 Odin3 固件提供的 `PServerBinder` 调用固定硬件命令、校验返回并读回；不要求电脑激活、不启动临时 ADB 桥，不拷贝原厂守护进程。
- 性能、风扇、灯光与充电控制限定合法操作和值，失败时恢复并如实提示。固件私有接口的适用范围见 [接入记录](docs/hardware-standalone-investigation.md)。
- 顶部 TAB 区相应缩小，在设置左侧显示电池电量、充电状态、风扇 RPM 与 PWM；底部 Dock 布局保持。
- 启动、滤镜状态和新增顶部状态区验收见 [本轮记录](docs/completion-validation.md)。历史待验描述仅表示当时状态；未复现的 Logo 根因仍单独跟踪。

---

## 三、 模块与包结构设计

```
com.odin.desktop
├── dashboard/           # 只读统计、外部存储卷与四项常用操作
├── data/
│   ├── db/              # Room 数据库配置 (OdinDatabase)
│   ├── entity/          # TabEntity, AppMappingEntity
│   ├── dao/             # TabDao, AppMappingDao
│   ├── model/           # InstalledApp
│   └── repository/      # AppRepository
├── service/
│   ├── afk/             # AfkTileService, AfkOverlayService, BurnInShifterEngine
│   └── fan/             # FanWatchdogService, FanController, HardwareController, AppMonitorAccessibilityService
├── receiver/            # BootCompletedReceiver (自启动后台守护)
└── ui/
    ├── MainActivity.kt  # 沉浸式 Default Home Launcher 宿主 & 全局手柄按键分发
    ├── navigation/      # FocusZone, GamepadKeyHandler
    ├── viewmodel/       # LauncherViewModel
    ├── screens/         # LauncherScreen
    ├── components/      # TopTabBar, AppHorizontalRow, AppCard, BottomDockBar, ConfigDialog, AppOptionsDialog
    └── theme/           # Color, Theme (纯黑 OLED 掌机主题)
```
